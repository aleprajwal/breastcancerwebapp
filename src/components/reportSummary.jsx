import React from 'react';
import {Link} from 'react-router-dom';

const ReportSummary = (props) => {
    return(<h1>
        This is report summary page.
        <Link to='/'>Home</Link>
    </h1>)
};

export default ReportSummary;